
      <%= simple_form_for([@pool, @reservation], url: pool_reservation_path, method: :patch) do |f| %>
        <%= f.error_notification %>
        <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>


        <div class="form-inputs">
        <%= f.input :start_date, label: 'date de reservation' %>
              <%= f.input :start_time, label: 'heure de début'%>
    <%= f.input :end_time, label: 'heure de fin' %>
          <% if @reservations.any?%>
          <%= f.input :owner_acceptation, label: "accepter la location" %>
          <% end %>
        </div>

        <div class="form-actions">

          <%= f.button :submit, class: "btn-primary" %>


</div>
<% end %>
